const http=require('http');const mode=process.env.LAB_MODE||'vulnerable';const docs={A:{id:'A',owner:'USER_A',body:'A-private'},B:{id:'B',owner:'USER_B',body:'B-private'}};
http.createServer((req,res)=>{const u=new URL(req.url,'http://local');const m=u.pathname.match(/^\/documents\/(A|B)$/);if(!m){res.writeHead(404);return res.end('not found')}const actor=req.headers['x-lab-user']||'USER_A',doc=docs[m[1]];if(mode==='fixed'&&doc.owner!==actor){res.writeHead(403,{'content-type':'application/json'});return res.end(JSON.stringify({error:'forbidden'}))}res.setHeader('content-type','application/json');res.end(JSON.stringify(doc));}).listen(8080);
