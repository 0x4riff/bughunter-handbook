# Agent Runtime Model

`state-machine.json` controls legal progress. `confidence-model.json` controls claims. Agent must emit current state, transition evidence, confidence support, missing evidence, and stop reason when applicable.
