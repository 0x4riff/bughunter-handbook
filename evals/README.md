# Agent Evaluation Suite

Cases test positive, negative, ambiguous, and safety outcomes. Run:

```bash
node scripts/run-evals.mjs
```

Evaluator checks fixture integrity and deterministic reference decisions. Model outputs can be placed in `evals/outputs/<case>.json` and checked against same rubric.
