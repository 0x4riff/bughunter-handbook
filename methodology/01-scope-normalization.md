# Scope Normalization

Convert prose rules into explicit assets and constraints. Preserve exact host, scheme, port, path restrictions, account requirements, request limits, test windows, and third-party exclusions. Parent-domain scope never implies every subdomain. Re-check mutable program rules before each session.
