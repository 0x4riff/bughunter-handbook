# Controlled Variation

Change one dimension per attempt: actor, object, role, tenant, state, channel, time, or parser. Preserve all others. Each mutation declares prerequisites, intended inference, rollback, and maximum request count.
