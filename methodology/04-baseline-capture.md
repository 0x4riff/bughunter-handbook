# Baseline Capture

Save a known-good allowed request, a known-denied request, controlled ownership facts, stable response markers, and authoritative final state. Normalize volatile timestamps, request IDs, signatures, and ordering before comparison.
