# Agent Finding Record

```yaml
status:
confidence_level:
security_invariant:
controlled_assets:
evidence_ids:
alternatives_checked:
authoritative_verification:
confirmed_impact:
unverified_limits:
stop_conditions_triggered:
cleanup:
```

Forbidden unsupported words: `all users`, `critical`, `account takeover`, `bypass`, `remote code execution`, or `data exfiltration` unless evidence proves exact claim.
